<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ahmedhamdan.flutter_uwb">

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true"/>
    <uses-feature android:name="android.hardware.uwb"           android:required="false"/>

    <!--
        Pre-Android-12 BLE permissions (BLUETOOTH, BLUETOOTH_ADMIN,
        ACCESS_FINE_LOCATION) are deliberately NOT declared here. A library
        manifest must never attach android:maxSdkVersion to a permission:
        the manifest merger copies that attribute onto the app's own
        declaration, silently stripping the permission on newer Android
        versions (e.g. an app using geolocator lost ACCESS_FINE_LOCATION
        on Android 12+ and could only request approximate location).
        Apps that support Android 11 or lower add these three permissions
        to their own manifest — see the README's Permissions section.
    -->

    <!-- Android 12+ runtime BLE permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"  tools:targetApi="s"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"    tools:targetApi="s"/>

    <!-- Android 14+ UWB permission -->
    <uses-permission android:name="android.permission.UWB_RANGING"          tools:targetApi="33"/>
</manifest>
